container_commands:
  01_install_server_deps:
    command: "cd server && npm install --production"

option_settings:
  aws:elasticbeanstalk:container:nodejs:
    NodeCommand: "cd server && node server.js"
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
